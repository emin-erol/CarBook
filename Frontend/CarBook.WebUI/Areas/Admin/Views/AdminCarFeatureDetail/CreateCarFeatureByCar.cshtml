@model List<ResultFeatureDto>

@{
    ViewData["Title"] = "CreateCarFeatureByCar";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}
<div class="page-content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Araca Ait Özellik Ekleme Sayfası</h4>
                        <p class="card-subtitle mb-4">
                            Araca ait özellik tanımlaması yapabilir, ilgili güncellemeleri
                            gerçekleştirebilirsiniz.
                        </p>
                        <form method="post">
                            <div class="container">
                                <div class="row">
                                    @for (int i = 0; i < Model.Count; i++)
                                    {
                                        if (i % 3 == 0 && i != 0)
                                        {
                                            <div class="row"></div>
                                        }
                                        <div class="col-md-4 mb-3">
                                            <div class="form-check">
                                                <input type="hidden" asp-for="@Model[i].FeatureId" />
                                                <input type="hidden" asp-for="@Model[i].Name" />
                                                <input type="checkbox" class="form-check-input"/>
                                                <label class="form-check-label">@Model[i].Name</label>
                                            </div>
                                        </div>
                                    }
                                </div>
                            </div>
                            <button class="btn btn-success mt-3">Araca Özellikleri Ata</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
